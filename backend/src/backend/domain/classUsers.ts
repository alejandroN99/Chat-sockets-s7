
export class User{
   private username: string;
   private password: string;

    constructor (username: string, password: string, sala: string) {
       this.username = username;
       this.password = password;
    }
};